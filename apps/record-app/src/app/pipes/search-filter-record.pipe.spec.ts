import { SearchFilterRecordPipe } from './search-filter-record.pipe';

describe('SearchFilterRecordPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchFilterRecordPipe();
    expect(pipe).toBeTruthy();
  });
});
